<form method="post" action="{{ path('calculator_delete', {'id': calculator.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calculator.id) }}">
    <button class="btn">Delete</button>
</form>
